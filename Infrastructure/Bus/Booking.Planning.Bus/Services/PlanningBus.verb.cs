namespace BookingKata.Infrastructure.Network;

public partial class PlanningBus
{
    private const string? Planning = null;

    public static string Recipient = nameof(Planning);

    public static class Verb
    {
        // public const string QuotationRequest = $"{nameof(Planning)}:{nameof(QuotationRequest)}";
        // public const string QuotationEmitted = $"{nameof(Planning)}:{nameof(QuotationEmitted)}";
    }
}